#include "World.h"

World::World(unsigned sizeX, unsigned sizeY, std::vector<float> biomeValues){
    
}

World::World(unsigned sizeX, unsigned sizeY, unsigned seed){
    
}

void World::draw(sf::RenderTarget& target, sf::RenderStates states) const{

}